# 2026-05-06 — Сессия 3: Фаза 2, Block B (Core API)

## Цель сессии

Запустить seed.py с демо-данными, затем закрыть Block B — Core API:
роутеры и Pydantic-схемы для сделок, документов, согласования.
Ключевые эндпоинты: `/deals`, `/deals/{id}`, `/documents/{id}`,
`/documents/{id}/approve`, `/dashboard`.

## Контекст на начало сессии

- Передан файл `ПЕРЕДАЧА-СЕССИЯ-3.md` и папка проекта целиком.
- Block A полностью закрыт: модели, LLM-клиент, health endpoints, seed.py.
- docker-compose поднят, все три `/health` отвечают ok.
- seed.py создан, но ещё не выполнялся — нужно запустить первым делом.

## Диалог сессии

---

### Запрос 1

**Jen:** Привет. Прикрепляю папку проекта и ПЕРЕДАЧА-СЕССИЯ-3.md.
Сделай: прочитай передачу, создай лог сессии, запусти seed.py, начни Block B.

**AI-агент:** Прочитал ПЕРЕДАЧА-СЕССИЯ-3.md. Создал лог сессии.
Docker недоступен из sandbox — дала команду пользователю запустить seed вручную.
Параллельно написан Block B (Core API) — все 6 файлов.

**Созданные файлы Block B:**
- `код/backend/app/модули/сделки/схемы.py` — Pydantic-схемы сделок
- `код/backend/app/модули/документы/схемы.py` — Pydantic-схемы документов
- `код/backend/app/модули/согласование/схемы.py` — Pydantic-схемы согласования
- `код/backend/app/модули/сделки/роутер.py` — `GET /deals`, `GET /deals/dashboard`, `GET /deals/{id}`
- `код/backend/app/модули/документы/роутер.py` — `GET/POST /deals/{id}/documents`, `GET /documents/{id}`
- `код/backend/app/модули/согласование/роутер.py` — `GET /documents/{id}/approvals`, `POST /documents/{id}/approve`
- **Обновлён** `код/backend/app/api/__init__.py` — зарегистрированы 3 новых роутера

**Команды проверки (запустить в терминале после seed):**
```bash
# Перезапустить контейнер чтобы подхватить новые файлы
docker compose restart backend

# Проверить эндпоинты
curl http://localhost:8000/deals
curl http://localhost:8000/deals/dashboard
curl http://localhost:8000/docs   # Swagger UI
```

---

### Запрос 2

**Jen:** http://localhost:8000/docs — всё открылось, все наименования вижу. Seed уже запускала.

**AI-агент:** Block B подтверждён работающим. Seed выполнен ранее — данные в БД есть.
Приступил к Block C (AI Layer) — написаны все 5 сервисов и AI роутер.

**Созданные файлы Block C:**
- `модули/документы/сервисы/извлечение.py` — LLM извлекает поля по типу документа
- `модули/документы/сервисы/поиск.py` — семантический поиск (pgvector cosine)
- `модули/сделки/сервисы/проверка.py` — проверка полноты пакета документов
- `модули/сделки/сервисы/задержки.py` — анализ просрочек и рисков виз
- `модули/документы/сервисы/сравнение.py` — сравнение версий документа
- `api/ai_роутер.py` — все AI эндпоинты
- **Обновлён** `api/__init__.py` — подключён ai_роутер

**AI-эндпоинты:**
```
POST /ai/documents/{id}/extract      — LLM извлекает поля
POST /ai/search                      — семантический поиск по документам
GET  /ai/deals/{id}/completeness     — проверка полноты пакета
GET  /ai/deals/{id}/delays           — анализ задержек и рисков
POST /ai/documents/compare           — сравнение двух версий документа
```

---

### Запрос 3

**Jen:** в Swagger разделе AI всё есть. Приступай к следующему шагу.

**AI-агент:** Block C подтверждён. Выполнен Block D (Frontend — Next.js 14 + Tailwind).

**Созданные файлы Block D:**
- `код/frontend/Dockerfile`
- `код/frontend/package.json` (Next.js 14, React 18, Tailwind 3)
- `код/frontend/next.config.mjs` (proxy /api/* → backend:8000)
- `код/frontend/tailwind.config.ts`, `postcss.config.js`, `tsconfig.json`
- `код/frontend/lib/api.ts` — типизированный API-клиент
- `код/frontend/lib/helpers.ts` — форматирование дат, сумм, статусов
- `код/frontend/app/globals.css` — базовые Tailwind-стили
- `код/frontend/app/layout.tsx` — навигация (Сделки / Дашборд / Поиск)
- `код/frontend/app/page.tsx` — redirect → /deals
- `код/frontend/app/deals/page.tsx` — список сделок
- `код/frontend/app/deals/[id]/page.tsx` — карточка + 3 вкладки (документы/согласование/AI)
- `код/frontend/app/documents/[id]/page.tsx` — просмотр документа + AI-извлечение
- `код/frontend/app/search/page.tsx` — семантический поиск
- `код/frontend/app/dashboard/page.tsx` — дашборд (KPI + статусы + события)
- **Обновлён** `docker-compose.yml` — добавлен сервис frontend (порт 3000)
- **Обновлён** `код/backend/app/главный.py` — добавлен CORS

**Команды запуска:**
```bash
cd /Users/user/Documents/ai-edm-закупки
docker compose up -d --build
# frontend: http://localhost:3000
```

---


