# Prompts 3 — AI-EDM Закупки

**Дата:** 2026-05-06  
**Сессия:** 3 — Фаза 2, Block B (Core API) + Block C (AI Layer) + Block D (Frontend)  
**Статус на старте:** Block A завершён, docker-compose работает, seed.py создан но не запущен

---

## Контекст

Продолжение после сессии 2. Block A полностью закрыт: модели, LLM-клиент, health endpoints, seed.py. Цель сессии — запустить seed.py и закрыть Blocks B, C, D: Core API, AI Layer, Frontend.

---

## Промты сессии

### Промт 1
> Привет. Прикрепляю папку проекта и ПЕРЕДАЧА-СЕССИЯ-3.md. Сделай: прочитай передачу, создай лог сессии, запусти seed.py, начни Block B.

### Промт 2
> http://localhost:8000/docs — всё открылось, все наименования вижу. Seed уже запускала.

### Промт 3
> в Swagger разделе AI всё есть. Приступай к следующему шагу.

### Промт 4
> Запустила `docker compose up -d --build`. Фронтенд открылся на localhost:3000. Сделки загружаются, карточка открывается. Но вкладка AI не работает — ошибка при извлечении полей.

### Промт 5
> Ошибка в логах: `NameError: name 'doc_id' is not defined` в роутере согласования.

### Промт 6
> После исправления — AI-вкладка заработала. Извлечение полей работает корректно.

### Промт 7
> Семантический поиск возвращает пустой массив. Запрос уходит, ответ приходит, но результатов нет.

### Промт 8
> Понял, нужно сначала создать эмбеддинги. Запустила backfill — отработал. Теперь поиск возвращает результаты.

---

## Лог работы

### Block B — Core API (ЗАВЕРШЁН ✅)

**Созданные файлы:**
- `модули/сделки/схемы.py` — Pydantic-схемы сделок
- `модули/документы/схемы.py` — Pydantic-схемы документов
- `модули/согласование/схемы.py` — Pydantic-схемы согласования
- `модули/сделки/роутер.py` — `GET /deals`, `GET /deals/dashboard`, `GET /deals/{id}`
- `модули/документы/роутер.py` — `GET/POST /deals/{id}/documents`, `GET /documents/{id}`
- `модули/согласование/роутер.py` — `GET /documents/{id}/approvals`, `POST /documents/{id}/approve`
- `api/__init__.py` — зарегистрированы все роутеры

---

### Block C — AI Layer (ЗАВЕРШЁН ✅)

**Созданные файлы:**
- `модули/документы/сервисы/извлечение.py` — LLM извлекает поля по типу документа
- `модули/документы/сервисы/поиск.py` — семантический поиск (pgvector cosine)
- `модули/сделки/сервисы/проверка.py` — проверка полноты пакета
- `модули/сделки/сервисы/задержки.py` — анализ просрочек и рисков виз
- `модули/документы/сервисы/сравнение.py` — сравнение двух версий договора
- `api/ai_роутер.py` — все AI-эндпоинты

**AI-эндпоинты:**
```
POST /ai/documents/{id}/extract      — LLM извлекает реквизиты
POST /ai/search                      — семантический поиск по документам
GET  /ai/deals/{id}/completeness     — проверка полноты пакета
GET  /ai/deals/{id}/delays           — анализ задержек и рисков
POST /ai/documents/compare           — сравнение двух версий договора
```

---

### Block D — Frontend (ЗАВЕРШЁН ✅)

**Созданные файлы:**
- `код/frontend/Dockerfile`
- `код/frontend/package.json` — Next.js 14, React 18, Tailwind 3
- `код/frontend/lib/api.ts` — типизированный API-клиент
- `код/frontend/lib/helpers.ts` — форматирование дат, сумм, статусов
- `код/frontend/app/layout.tsx` — навигация (Сделки / Дашборд / Поиск)
- `код/frontend/app/deals/page.tsx` — список сделок
- `код/frontend/app/deals/[id]/page.tsx` — карточка сделки (3 вкладки)
- `код/frontend/app/documents/[id]/page.tsx` — просмотр документа + AI
- `код/frontend/app/search/page.tsx` — семантический поиск
- `код/frontend/app/dashboard/page.tsx` — дашборд с KPI и событиями
- `docker-compose.yml` — добавлен сервис frontend (порт 3000)
- `app/главный.py` — добавлен CORS

**Исправленный баг:**
`NameError: name 'doc_id' is not defined` в `согласование/роутер.py` — переменная `документ_id` использовалась вместо path-параметра `doc_id` в трёх местах. Исправлено.
